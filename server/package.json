{
  "name": "inventrack-server",
  "version": "1.0.0",
  "description": "Backend for Inventrack",
  "main": "dist/index.js",
  "scripts": {
    "build": "npm run clean && tsc",
    "clean": "rimraf dist",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node -r ts-node/register -r dotenv/config src/index.ts"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.14.8",
    "dotenv": "^16.4.5",
    "rimraf": "^6.0.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.2"
  },
  "dependencies": {
    "@fastify/autoload": "^6.0.0",
    "@fastify/type-provider-typebox": "^5.0.0",
    "fastify": "^5.0.0",
    "pino": "^9.4.0",
    "pino-pretty": "^11.2.2",
    "shared": "../shared"
  }
}
