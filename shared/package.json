{
  "name": "shared",
  "private": true,
  "version": "0.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npm run compile",
    "compile": "npm run clean && tsc",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@sinclair/typebox": "^0.33.12",
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.0.2"
  }
}
